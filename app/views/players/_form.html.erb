<%= form_for [@match, @player] do |f| %>
	
	<div class="field">
		<%= f.label :username %>
		<%= f.text_field :username %>
	</div>
	
	<div class="action">
		<%= f.submit "Join the Match", :class => "btn btn-primary" %>
	</div>
	
<% end %>